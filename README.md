# orchestration-template

A reusable, project-agnostic orchestration harness for driving multi-stage,
dependency-aware coding tasks through Claude (and Codex) — built to run as an
in-session Workflow on the subscription, and headless via the Agent SDK.

Status: **pre-build.** Spec extraction in progress. See `docs/`.
