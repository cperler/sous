{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "scope",
  "description": "Stage output contract: feasibility decision plus a minimal task plan.",
  "type": "object",
  "required": ["feasible", "plan"],
  "properties": {
    "feasible": {"type": "boolean"},
    "blocked_reason": {"type": ["string", "null"], "description": "why the task is blocked, if not feasible"},
    "plan": {
      "type": "array",
      "items": {"type": "string"},
      "description": "ordered subtasks for the implement stage"
    },
    "files": {
      "type": "array",
      "items": {"type": "string"},
      "description": "repo-relative paths this task expects to MODIFY (not merely read); the engine serializes tasks whose declared files collide (#377). Optional: a task that declares nothing neither defers nor blocks."
    },
    "subtasks": {
      "type": "array",
      "minItems": 1,
      "description": "optional child-task DAG; when present the current task becomes an umbrella",
      "items": {
        "type": "object",
        "required": ["id", "description"],
        "properties": {
          "id": {"type": "string", "minLength": 1, "maxLength": 80, "pattern": "^\\S+$"},
          "description": {"type": "string", "minLength": 1},
          "agent": {"type": ["string", "null"]},
          "quality_tier": {"type": "string", "enum": ["full", "light", "none"], "default": "full"},
          "implementation_budget": {"type": "string", "enum": ["standard", "short"], "default": "standard"},
          "depends_on": {"type": "array", "items": {"type": "string"}, "default": []}
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": true
}
