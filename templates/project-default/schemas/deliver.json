{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "deliver",
  "description": "Stage output contract: docstrings refreshed and the task branch pushed to the remote. Opening the PR is the publish stage's job (#389).",
  "type": "object",
  "required": ["branch", "pushed_head_sha"],
  "properties": {
    "branch": {"type": "string"},
    "pushed_head_sha": {"type": "string"}
  },
  "additionalProperties": true
}
