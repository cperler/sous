"""The package version.

Deliberately trivial. This module exists so the skeleton has *something* real to import,
type-check, and test — the first orchestrated task adds files beside it and will follow
whatever layout it finds here.
"""

from __future__ import annotations

__version__ = "0.1.0"


def version() -> str:
    """Return the package version."""
    return __version__
